# Firebase CLI usa este archivo en lugar de .gitignore si existe.
# No listar .next aquí: si haces npm run build antes de deploy, .next debe subirse.
.git
.gitignore
node_modules
*.md
.env*.local
.firebase
